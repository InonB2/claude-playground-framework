[2026-05-03] ALERT — All 3 URLs returned HTTP 403 (Forbidden).

Failed (non-2xx): Portfolio (403), TradePulse (403), FamilyFlow (403)
OK (2xx/3xx): None
Supabase URLs: None configured (infra/keep_alive_urls.txt has no active entries)

NOTE: All servers ARE responding (TLS handshake successful, 403 returned by CDN/bot protection).
This is likely Cloudflare or platform-level bot protection blocking curl User-Agent — NOT a real outage.
Verify in browser. If apps load normally, update monitoring to use a browser-like User-Agent or
accept 403 as UP for these hosted platforms.

Action needed: Confirm apps are accessible in browser. If yes, no action required.
If browser also returns errors, check hosting dashboard at base44.app / lovable.app.
