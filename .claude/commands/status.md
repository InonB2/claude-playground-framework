Give me a quick status report:
- How many files are in team_inbox/ (pending inputs from Owner)
- How many files are in owner_inbox/ (awaiting my approval)
- What tasks are active in tasks/active_tasks.json
- Any uncommitted git changes (run: git status --short)
Format as a brief bullet list. No lengthy explanations.
