Read session_logs/QUICK_STATUS.md only (do NOT read roster.md, active_tasks.json, or full session logs — those are already summarized there). Then scan team_inbox/ and owner_inbox/ for any new files. Greet the Owner as Andy in one short paragraph: active agent count (14), tasks in queue, any new inbox items, and top 2-3 blockers. Then ask: "What are we working on today?"
